# Project Context — testing-app

> Auto-generated by the Codebase Analyzer. Do not edit manually.
> Last updated: 2026-05-04T00:00:00.000Z

## 1. Project Profile

| Aspect | Value |
|---|---|
| **Type** | SPA (Single Page Application) |
| **Framework** | React 19.2.5 |
| **Language** | TypeScript ~6.0.2 (strict mode via noUnusedLocals, noUnusedParameters, erasableSyntaxOnly) |
| **Build Tool** | Vite 8.0.10 |
| **Package Manager** | npm (lockfile: package-lock.json) |
| **Node Version** | Not specified (no engines field or .nvmrc) |
| **Module System** | ESM (`"type": "module"`) |

## 2. Architecture

```
src/
  main.tsx              # App entry point — renders <App /> inside React.StrictMode
  App.tsx               # Router configuration — owns all route definitions
  index.css             # Global styles + Tailwind v4 import (@import "tailwindcss")
  components/           # Reusable UI components (layouts, cards, nav items)
    DashboardLayout.tsx # Dashboard shell: sidebar, header, main content area + NavItem helper
  pages/                # Route-level page components
    LoginPage.tsx       # Login page with email/password form (UI-only, no auth logic yet)
    DashboardPage.tsx   # Dashboard overview with stats, chart placeholder, transactions + StatCard/TransactionItem helpers
  assets/               # Static assets (images, SVGs)
    hero.png            # Hero image
    react.svg           # React logo
    vite.svg            # Vite logo
```

### Folder Purpose

| Folder | Purpose |
|---|---|
| `src/` | Root source directory for all application code |
| `src/components/` | Reusable UI components — layouts, cards, nav items. Co-located helper components allowed. |
| `src/pages/` | Route-level page components — one per route. Named `*Page.tsx`. |
| `src/assets/` | Static assets — images, SVGs, fonts referenced by components |
| `public/` | Public static files served as-is (favicon.svg) |

## 3. Routing

- **Library**: react-router-dom 7.14.2
- **Entry point**: `src/App.tsx`
- **Route table**:

| Path | Component | Type |
|---|---|---|
| `/` | `<Navigate to="/login" replace />` | Default redirect |
| `/login` | `<LoginPage />` | Public |
| `/dashboard` | `<DashboardLayout><DashboardPage /></DashboardLayout>` | Protected (layout-wrapped) |

- **Layout pattern**: Protected routes are wrapped with layout components directly in the `element` prop (e.g., `<DashboardLayout><DashboardPage /></DashboardLayout>`).
- **Route protection**: No actual auth guards implemented yet. Layout wrapping implies intended protection pattern but no token/session verification exists.
- **Redirects**: Root `/` redirects to `/login` via `<Navigate replace />`.
- **Dynamic routes**: None currently defined.

## 4. State Management

- **Approach**: Local state only (no global state management library).
- **Global state providers**: None.
- **Custom state hooks**: None (`src/hooks/` does not exist).
- **Data fetching**: None implemented (no API calls, no React Query, no SWR).
- **State patterns**: Components currently use no `useState` or `useEffect` — all UI is static/mock data.

## 5. API / Service Layer

- **HTTP client**: None configured.
- **Base URL configuration**: Not applicable.
- **Service modules**: None (`src/services/` and `src/api/` do not exist).
- **Endpoint patterns**: Not applicable.
- **Note**: The login form has `onSubmit={(e) => e.preventDefault()}` — no actual authentication logic exists yet.

## 6. Styling

- **Framework**: Tailwind CSS 4.2.4
- **Import style**: `@import "tailwindcss"` in `src/index.css` (Tailwind v4 syntax)
- **PostCSS pipeline**: `@tailwindcss/postcss` + `autoprefixer`
- **Theme**: No custom theme extensions in `tailwind.config.js` — uses default Tailwind palette.
- **Dark theme palette** (observed in components):
  - Background: `bg-slate-950`
  - Surfaces: `bg-slate-900/50`, `bg-slate-900/40`, `bg-slate-800`
  - Borders: `border-slate-800`, `border-slate-700`
  - Text: `text-white`, `text-slate-200`, `text-slate-400`, `text-slate-500`
  - Accents: `bg-indigo-600`, `text-indigo-400`, `focus:ring-indigo-500/50`
  - Success: `text-emerald-400`
  - Danger: `text-rose-400`
- **Global styles** (`src/index.css`):
  - Font family: Inter, system-ui, Avenir, Helvetica, Arial, sans-serif
  - Base background: `#242424`
  - Color scheme: `light dark`
  - Body: flex-centered layout, min-width 320px, min-height 100vh
  - `#root`: full width
- **Component styling pattern**: Tailwind utility classes exclusively. No inline styles. No CSS modules. No styled-components.
- **Responsive breakpoints**: Standard Tailwind (`md:`, `lg:`, `sm:`). Observed usage: `md:col-span-2`, `lg:col-span-4`, `hidden md:flex`, `hidden sm:block`.
- **Animation**: `animate-in fade-in duration-700` on DashboardPage root. `transition-all`, `transition-colors`, `transition-transform`, `transition-opacity` used for interactive states.

## 7. Validation

- **Form library**: None.
- **Schema validation**: None (no Zod, Yup, or Valibot).
- **Validation pattern**: Manual client-side validation expected but not yet implemented. Forms use `onSubmit={(e) => e.preventDefault()}` as placeholder.
- **Note**: Governance rules require all form inputs to be validated client-side before submission — this is a gap to address when adding auth logic.

## 8. Testing

- **Framework**: None configured.
- **Test runner**: None.
- **Component testing**: None.
- **E2E testing**: None.
- **Test file convention**: Not applicable.
- **Coverage**: Not configured.
- **Governance expectation**: When tests are added, use Vitest + React Testing Library (aligns with Vite ecosystem).

## 9. Build and Deployment

- **Build command**: `npm run build` → `tsc -b && vite build`
- **Dev command**: `npm run dev` → `vite`
- **Lint command**: `npm run lint` → `eslint .`
- **Preview command**: `npm run preview` → `vite preview`
- **Output directory**: `dist/` (gitignored)
- **CI/CD**: None configured (no `.github/workflows/`).
- **Containerization**: None (no Dockerfile).

## 10. Coding Conventions

### Naming

- **Components**: PascalCase with descriptive suffixes (`LoginPage.tsx`, `DashboardPage.tsx`, `DashboardLayout.tsx`)
- **Pages**: `*Page.tsx` pattern in `src/pages/`
- **Layouts**: `*Layout.tsx` pattern in `src/components/`
- **Helper components**: PascalCase, co-located in same file (`StatCard`, `TransactionItem`, `NavItem`)
- **Hooks**: `use*` prefix convention (no hooks exist yet)
- **Utilities**: camelCase (no utilities exist yet)
- **Types/Interfaces**: `*Props` pattern (e.g., `LayoutProps`)

### Component Rules

- **Type annotation**: `React.FC` for all components (including pages and layouts).
- **Props typing**: Separate `interface` declarations for layout components (`interface LayoutProps`). Inline types for small helper components (`{ title: string, value: string, ... }`).
- **Exports**: `export default` from each file.
- **Co-location**: Small helper components used by only one parent are co-located in the same file (e.g., `StatCard` and `TransactionItem` in `DashboardPage.tsx`, `NavItem` in `DashboardLayout.tsx`).
- **Size limit**: Components observed are under ~150 lines. `DashboardPage.tsx` is 129 lines, `DashboardLayout.tsx` is 101 lines, `LoginPage.tsx` is 74 lines.

### Import Rules

- **Ordering**: External libraries first (`react`, `lucide-react`, `react-router-dom`), then internal imports (`./pages/...`, `./components/...`).
- **Path style**: Relative imports only (no path aliases configured in Vite or TypeScript).
- **Default vs named**: Default imports for React and internal modules. Named imports for icons from `lucide-react` (e.g., `{ Lock, Mail, ArrowRight }`).

### Code Quality

- **TypeScript strictness**:
  - `noUnusedLocals: true`
  - `noUnusedParameters: true`
  - `erasableSyntaxOnly: true`
  - `noFallthroughCasesInSwitch: true`
  - `verbatimModuleSyntax: true`
  - `strict` mode not explicitly set but effective strictness via above flags.
- **ESLint rules** (flat config):
  - `@eslint/js` recommended
  - `typescript-eslint` recommended
  - `eslint-plugin-react-hooks` recommended
  - `eslint-plugin-react-refresh` for Vite
  - Ignores `dist/` directory
- **Forbidden patterns** (from governance):
  - No `any` types
  - No `console.log` in production code
  - No inline styles (Tailwind only)
  - No `dangerouslySetInnerHTML` without sanitization
  - No `eval()` or `Function()`
  - No hardcoded secrets

## 11. Dependency Usage

### Active Dependencies

| Package | Version | Usage |
|---|---|---|
| `react` | ^19.2.5 | UI framework — `React.FC`, `React.ReactNode`, `StrictMode` |
| `react-dom` | ^19.2.5 | DOM rendering — `createRoot` |
| `react-router-dom` | ^7.14.2 | Routing — `BrowserRouter`, `Routes`, `Route`, `Navigate` |
| `lucide-react` | ^1.14.0 | Icons — named imports, `size` prop and `className` for styling |

### Dev Dependencies

| Package | Version | Usage |
|---|---|---|
| `typescript` | ~6.0.2 | Type checking |
| `vite` | ^8.0.10 | Build tool and dev server |
| `@vitejs/plugin-react` | ^6.0.1 | Vite React plugin (Babel/JSX transform) |
| `eslint` | ^10.2.1 | Linting |
| `@eslint/js` | ^10.0.1 | ESLint recommended JS rules |
| `typescript-eslint` | ^8.58.2 | TypeScript ESLint integration |
| `eslint-plugin-react-hooks` | ^7.1.1 | React hooks linting |
| `eslint-plugin-react-refresh` | ^0.5.2 | Vite HMR compatibility |
| `globals` | ^17.5.0 | Browser globals for ESLint |
| `tailwindcss` | ^4.2.4 | CSS framework |
| `@tailwindcss/postcss` | ^4.2.4 | Tailwind PostCSS plugin (v4) |
| `postcss` | ^8.5.13 | CSS transformation pipeline |
| `autoprefixer` | ^10.5.0 | Vendor prefixing |
| `@types/react` | ^19.2.14 | TypeScript types for React |
| `@types/react-dom` | ^19.2.3 | TypeScript types for ReactDOM |
| `@types/node` | ^24.12.2 | Node.js types (for Vite config) |

### Declared but Unused

| Package | Version |
|---|---|
| None detected — all declared dependencies are actively used. | |

### Framework Patterns

- **React hooks used**: None currently (`useState`, `useEffect`, `useMemo`, `useCallback`, `useRef` not yet used). Components are purely presentational.
- **Router components used**: `BrowserRouter`, `Routes`, `Route`, `Navigate`. Not yet used: `Link`, `useNavigate`, `useParams`, `useLocation`, `Outlet`.
- **Icon library usage**: Named imports from `lucide-react`. Icons accept `size` prop (number) and `className` for color/styling. Examples: `<Lock className="text-white h-6 w-6" />`, `<Menu size={24} />`.
- **Tailwind patterns**:
  - Responsive breakpoints: `sm:`, `md:`, `lg:`
  - Opacity modifiers: `bg-slate-900/50`, `bg-indigo-600/20`, `focus:ring-indigo-500/50`
  - Backdrop blur: `backdrop-blur-xl`, `backdrop-blur-md`
  - Hover states: `hover:bg-indigo-500`, `hover:text-white`, `hover:border-indigo-500/50`
  - Focus states: `focus:outline-none`, `focus:ring-2`, `focus:border-indigo-500`
  - Group hover: `group-hover:opacity-100`, `group-hover:translate-x-1`
  - Transitions: `transition-all`, `transition-colors`, `transition-transform`, `transition-opacity`, `duration-200`, `duration-300`, `duration-700`
  - Custom shadow: `shadow-[0_0_8px_rgba(129,140,248,0.8)]`

## 12. Environment Configuration

- **Environment files**: None present (no `.env`, `.env.local`, `.env.production`).
- **Vite env prefix**: `VITE_*` (standard Vite convention, not yet used).
- **Env var usage in code**: None. No `import.meta.env` references found.

## 13. Project-Specific Rules

### File/Folder Modification Rules (from governance-agent.md)

1. Do NOT modify `vite.config.ts`, `tsconfig*.json`, `eslint.config.js`, or `tailwind.config.js` without explicit user approval.
2. New pages go in `src/pages/` as `*Page.tsx`.
3. New components go in `src/components/` — create subdirectories when a feature grows beyond 3 components.
4. New routes must be registered in `App.tsx`.
5. Do NOT restructure the existing folder layout without approval.
6. Assets go in `src/assets/`.
7. Never commit to `dist/` — it is gitignored.
8. Never commit `node_modules/`.

### Dependency Addition Rules

1. Approved dependencies (already in use): `react`, `react-dom`, `react-router-dom`, `lucide-react`, `tailwindcss`.
2. New dependencies require clear justification, preference for well-maintained packages, type definitions, and bundle size awareness.
3. Never add dependencies that introduce a different UI framework, replace Tailwind CSS, or add a state management library without approval.
4. Dev dependencies for testing, linting, or formatting are acceptable if justified.

### Security Rules

1. No hardcoded credentials — use `import.meta.env.VITE_*`.
2. XSS prevention — no `dangerouslySetInnerHTML` without sanitization.
3. Route protection — authenticated routes must verify session/token before rendering.
4. Input validation — all form inputs must be validated client-side before submission.
5. No `eval()` or `Function()`.
6. External URLs must use `rel="noopener noreferrer"`.
7. Prefer local assets for images; external URLs must be whitelisted/trusted.
8. Never use `localStorage`/`sessionStorage` for sensitive data without encryption.

### Code Quality Rules

1. TypeScript strictness — no `any`. All props, state, and function signatures must be typed.
2. No unused code — `noUnusedLocals` and `noUnusedParameters` are enforced.
3. ESLint compliance — all code must pass `npm run lint`.
4. No `console.log` in production code.
5. Component size — keep under ~150 lines. Extract sub-components when larger.
6. File naming — PascalCase for components, camelCase for utilities.
7. Import ordering — external libraries first, then internal.
8. No inline styles — use Tailwind classes exclusively.
9. Accessibility — all interactive elements must have proper `aria-*` attributes, labels, and keyboard support.
10. Form handling — use controlled components or a form library. Never leave `onSubmit` without proper handling.

### Review Checklist

- [ ] `npm run lint` passes with zero errors
- [ ] `npm run build` succeeds
- [ ] No `any` types introduced
- [ ] No `console.log` / `debugger` statements
- [ ] All new components typed with props interfaces
- [ ] New routes registered in `App.tsx`
- [ ] Tailwind classes used (no custom CSS or inline styles)
- [ ] No hardcoded secrets
- [ ] File naming follows PascalCase for components
- [ ] Imports are ordered and clean
- [ ] Component files under ~150 lines
- [ ] Accessibility basics covered
- [ ] No unrelated files modified

### Agent Instructions

1. Always read the full file before editing. Never make partial assumptions.
2. Preserve existing patterns — match code style, naming, and structure.
3. Minimize scope — only change what the user asked for.
4. Verify before output — mentally walk through the diff.
5. Fail fast — if a request conflicts with governance rules, explain and ask for clarification.
6. No speculative features — do not add unrequested functionality.
7. No config changes — do not modify build, lint, or type configs without explicit instruction.
8. Commit messages — use conventional commit format: `feat:`, `fix:`, `refactor:`, `chore:`, `test:`.

## 14. File Modification Map

| Change Type | Target Location |
|---|---|
| New page | `src/pages/<Name>Page.tsx` |
| New component | `src/components/<Name>.tsx` |
| New layout | `src/components/<Name>Layout.tsx` |
| New route | Register in `src/App.tsx` |
| New asset | `src/assets/` |
| New hook | `src/hooks/use<Name>.ts` (directory does not exist yet — create if needed) |
| New utility | `src/utils/<name>.ts` (directory does not exist yet — create if needed) |
| New service | `src/services/<name>.ts` (directory does not exist yet — create if needed) |
| New type | `src/types/<name>.ts` (directory does not exist yet — create if needed) or inline in component file |
| New constant | `src/constants/<name>.ts` (directory does not exist yet — create if needed) or inline in component file |

## 15. Gaps and Unclear Areas

1. **No authentication logic**: The login page is UI-only. No auth state, token management, or protected route guards exist.
2. **No API layer**: No services, HTTP client, or data fetching is implemented. All data is hardcoded/mock.
3. **No state management**: No global state, context, or custom hooks exist yet.
4. **No testing**: No test framework, test files, or testing utilities are configured.
5. **No environment variables**: No `.env` files or `import.meta.env` usage found.
6. **No CI/CD**: No GitHub Actions or deployment pipeline configured.
7. **External image URL**: `DashboardLayout.tsx` uses `https://ui-avatars.com/api/` for avatar — this is an external dependency not yet whitelisted per governance rules.
8. **`href="#"` links**: Multiple placeholder links use `href="#"` without proper `rel="noopener noreferrer"` or click handlers.
9. **No `Outlet` usage**: Routes use direct component wrapping instead of React Router's `<Outlet>` pattern — this works but may limit nested routing flexibility.
10. **No path aliases**: All imports use relative paths. No `@/` or `src/` alias configured in Vite or TypeScript.
11. **No form validation**: Login form has no validation logic despite governance requiring client-side validation.
12. **No TypeScript `strict` flag**: While individual strict flags are set, the `strict: true` umbrella flag is not explicitly enabled in `tsconfig.app.json`.

---

*This file is auto-generated. Do not edit manually.*
